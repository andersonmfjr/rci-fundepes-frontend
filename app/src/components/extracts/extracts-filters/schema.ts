export type FilterSchema = {
  mes_referencia: {
    stringValue: string;
    numberValue: number;
  };
  ano_referencia: {
    stringValue: string;
    numberValue: number;
  };
  id_conta_rci: string;
  status: string;
};
